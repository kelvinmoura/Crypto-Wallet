<p id="notice"><%= notice %></p>
<h1 align="center">Moedas</h1>

<table align="center" style="background-color: green;">
  <thead>
    <tr>
      <th><%= @coins.human_attribute_name(:description)%></th>
      <th><%= @coins.human_attribute_name(:acronym)%></th>
      <th><%= @coins.human_attribute_name(:url_image)%></th>
      <th><%= @coins.human_attribute_name(:mining_type)%></th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @coins.each do |coin| %>
      <tr>
        <td><%= coin.description %></td>
        <td><%= coin.acronym %></td>
        <td><%= image_tag coin.url_image, width: 25, height: 25 %></td>
        <td><%= coin.mining_type.acronym %></td>
        <td><%= link_to t("links.show"), coin %></td>
        <td><%= link_to t("links.edit"), edit_coin_path(coin) %></td>
        <td><%= link_to t("links.delete"), coin, method: :delete, data: { confirm: 'Tem certeza que seja apagar essa moeda?' } %></td>
      </tr>
    <% end %>
      <tr>
      <td><%= link_to 'Nova Moeda', new_coin_path %></td>
      <td><%= link_to 'Tipos de mineração', mining_types_path %></td>
      <td><%= link_to "Inicio", inicio_path %></td>
      </tr>
  </tbody>
</table>
